---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes <"a"> {
    variant: "primary" | "secondary";
}
const { variant, ...props } = Astro.props;
---

<div class="button">
    <a
        class:list={[
            "button",
            { primary: variant === "primary" },
            { secondary: variant === "secondary" },
            Astro.props.class,
        ]}
    >
        <slot />
    </a>
</div>

<style>
    .button {
        display: inline-block;
        border-radius: 100px;
        padding: 0.5rem 1.5rem;
        text-align: left;
        border:1px solid transparent;
    }

    .primary {
        background-color: var(--pj_brand_blue);
        border: 1px solid var(--pj_brand_blue);
    }

    .secondary {
        background-color: var(--pj_brand_blue);
        border: 1px solid var(--pj_blue_10);
    }

    .secondary:hover {
        background-color: var(--pj_brand_dark_blue);
        border: 1px solid var(--pj_brand_dark_blue);
    }

    .button a {
        text-size: 0.875rem;
        font-weight: 400;
        color: white;
        text-decoration: none;
        transition: background-color 0.3s ease;
    }

    .button a:hover {
        background-color: var(--pj_brand_dark_blue);
    }
</style>
